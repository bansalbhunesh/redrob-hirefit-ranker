# Redrob Hackathon - Submission Metadata

team_name: "redrob-hirefit-ranker"

primary_contact:
  name: "Bhunesh Bansal"
  email: "bhuneshbansal20039888@gmail.com"
  phone: "+91-XXXXXXXXXX"

team_members:
  - name: "Bhunesh Bansal"
    email: "bhuneshbansal20039888@gmail.com"
    role: "ML Engineer"

github_repo: "https://github.com/bansalbhunesh/redrob-hirefit-ranker"
sandbox_link: "https://huggingface.co/spaces/bansal1234/redrob-hirefit-ranker"
reproduce_command: "PYTHONHASHSEED=0 python rank.py --candidates ./candidates.jsonl --out ./submission.csv  # Docker image pins the seed automatically"

compute:
  platform: "Local CPU machine"
  cpu_cores: 8
  ram_gb: 16
  python_version: "3.14.3 local verification; compatible with 3.11+"
  os: "Windows"
  uses_gpu_for_inference: false
  has_network_during_ranking: false
  pre_computation_required: false
  pre_computation_time_minutes: 0
  # Feature scoring is parallelised across CPU workers (--workers, default auto).
  # Observed 100K range ~123-184s across runs on the dev machine (8 cores, Py 3.14);
  # output is identical to --workers 1. Re-measure in the 3.11 Docker image before submit.
  measured_runtime_seconds: 184
  measured_peak_rss_gb: 4.33
  measured_bm25_backend: "bm25s"
  feature_scoring_workers: "auto (up to 8)"

ai_tools_used:
  - "Codex"
  - "Claude Code (Anthropic) for runtime parallelisation, independent eval harness, soft JD penalties, reasoning variety"
  - "Claude/Kimi audit notes as design-review references"

ai_usage_summary: |
  Used Codex for architecture planning, implementation scaffolding, documentation,
  and test generation. Claude Code (Anthropic) did a later engineering pass:
  profiling and parallelising feature scoring for runtime margin, building the
  independent (non-circular) evaluation harness, adding the LLM-wrapper and
  junior-for-senior soft JD penalties, and improving reasoning variety; all
  verified against the test suite and the format validator. Claude/Kimi audit
  notes were used as offline design-review references. No candidate data is sent
  to hosted LLM APIs during ranking.

methodology_summary: |
  Offline Plan D v2 ranker for the Senior AI Engineer JD. BM25 provides lexical
  fit (bm25s preferred, rank-bm25 fallback), while a deterministic 28-feature
  matrix scores skills, career, experience, behavior, and logistics. The final
  score applies multiplicative behavioral, honeypot, and JD-disqualifier
  multipliers. Salary inversion and endorsement inflation are treated as soft
  disqualifier signals rather than hard honeypots. No hosted LLM/API scoring is
  used during ranking; reasoning is generated from actual candidate facts and
  feature triggers. A heuristic silver-label harness is included for development
  and defense, not as a claim about the hidden challenge labels.

declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true
  reproduction_tested: true
